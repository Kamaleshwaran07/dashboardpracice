import React from "react";

const Card = ({ item }) => {
    return (
      <div className="">
            
    {/* <div className="card " id="card">
      <img src={item.image}></img>

      <h3>{item.title}</h3>
      <p>{item.description}</p>
      <p>{item.date}</p>
    </div> */}
      </div>
  );
};

export default Card;
